

services:
  web:
    build: .
    container_name: doctor_appointment_django
    command: gunicorn --bind 0.0.0.0:8000 DoctorAppointment.wsgi:application
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env